---
## This playbook deploys the whole application stack in this site.  

# Apply common configuration to all hosts
- hosts: all
  
  roles:
  - {role: 'killChunks', tags: 'killChunks'}  # to use the tag, use this option: --tags "killChunks"


